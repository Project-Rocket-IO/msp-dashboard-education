from django.urls import path
from dashboards.views import (
    dashboard_analytics_view
)

app_name = 'dashboards'

urlpatterns = [
    # path('dashboard/',view =dashboard_analytics_view,name="dashboard"),
]


